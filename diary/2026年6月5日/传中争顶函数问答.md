问：python调用C++黑盒，必走的第一个函数是哪个？
答：black_box::next_frame

问：black_box::next_frame的主要功能是什么？
答：
    1. 浮点检查；
    2. 暂停逻辑；
    3. this->_Update();
    4. this->logic_black_box->tick_0(this->_frame_info->cur_frame_id, this->_frame_info->game_time);
    5. 所有entity，__tick_0_component(); update(); __tick_1_component()
    6. this->_Tick1_AttributeManager();
    7. this->_LateTick0();
    8. 所有entity，__late_tick_0_component；
    9. this->_LateTick1();
    10. 所有entity，__late_tick_1_component
    11. this->logic_black_box->next_frame(this->_frame_info->cur_frame_id, this->_frame_info->game_time);
    12. this->_LateUpdate();
    13. 所有entity，__late_update_component
    14. this->logic_black_box->late_update(this->_frame_info->cur_frame_id, this->_frame_info->game_time);
    15. this->_LateUpdate_DistManager();
	16. this->_LateUpdate_Rp3d();

问：在black_box::next_frame中，logic_black_box一共执行了三个函数，分别是：
答：
    1. tick_0
    2. next_frame
    3. late_update

问：在black_box::next_frame中，entity的更新一共有多少种？
答：
    1. update()

问：在black_box::next_frame中，entity的组件的更新一共有多少种？
答：
    1. __tick_0_component
    2. __tick_1_component
    3. __late_tick_0_component
    4. __late_tick_1_component
    5. __late_update_component

问：在black_box::next_frame中，black_box对自身的更新一共有多少种？
答：
    1. _Update();
    2. _Tick1_AttributeManager();
    3. _LateTick0();
    4. _LateTick1();
    5. _LateUpdate();
    6. _LateUpdate_DistManager();
    7. _LateUpdate_Rp3d();

问：LogicBlackBox::next_frame 和 LogicBlackBox::late_update 之间差了多少逻辑？
答：
    1. 黑盒本身的_LateUpdate
    2. 黑盒所有活着的entity的_late_update_component

问：black_box和LogicBlackBox之间的关系是什么？
答：
    都是类；可见代码命名规范并不统一；
    black_box拥有LogicBlackBox；LogicBlackBox是


问：logic_black_box的tick_0函数主要负责什么逻辑？
答：
    1. this->game_process_mgr->on_tick();
    2. this->game_rules_manager->tick_0(cur_frame_id, game_time);
    3. this->logic_ball->tick_0(cur_frame_id, game_time);
    如果没有切到定点动画框架，到这里就结束了。
    4. this->tick_batch_nodeplayer(cur_frame_id, game_time);
    5. 街球模式：对于所有球员：this->street_play->tick(cur_frame_id, game_time, player);
    6. this->tick_combat_component(cur_frame_id, game_time);

问：logic_black_box的next_frame函数主要负责什么逻辑？
答：
    1. 

问：logic_black_box的late_update函数主要负责什么逻辑？
答：

问：在black_box::next_frame中enity的update更新主要负责什么逻辑？
答：

问：在black_box::next_frame中enity的component为什么要区分出tick_0和tick_1？
答：

问：在black_box::next_frame中enity的component为什么要区分出late_tick_0和late_tick_1？
答：

问：在black_box::next_frame中enity的component的__late_update_component更新主要负责什么逻辑？
答：

问：在black_box::next_frame中，black_box对自身的更新的_Update()主要负责什么逻辑？
答：

问：在black_box::next_frame中，black_box对自身的更新的__tick1_attributemanager()主要负责什么逻辑？
答：

问：在black_box::next_frame中，black_box对自身的更新的__lateTick0()主要负责什么逻辑？
答：

问：在black_box::next_frame中，black_box对自身的更新的__lateTick1()主要负责什么逻辑？
答：

问：在black_box::next_frame中，black_box对自身的更新的__lateUpdate()主要负责什么逻辑？
答：

问：在black_box::next_frame中，black_box对自身的更新的__lateUpdateDistManager()主要负责什么逻辑？
答：

问：在black_box::next_frame中，black_box对自身的更新的__lateUpdateRp3d()主要负责什么逻辑？
答：

